version: "3.9"

services:
  api:
    build: .
    container_name: upstox-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - upstox-data:/data
    restart: unless-stopped

volumes:
  upstox-data:
